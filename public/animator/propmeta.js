// The prop channel list, mirrored from server/anim/store.js so the editor and
// the sanitizer cannot disagree about what a prop can animate.
export const PROP_CHANNELS = ['px', 'py', 'pz', 'rx', 'ry', 'rz', 'sc'];
